---
id: watcom/watcom-lineage
title: 拿到一支 Watcom 編譯的程式，怎麼判斷是哪一代
libraries: [watcom-6.5, watcom-7.0, watcom-9.01]
goals: [re, craft]
evidence: 強推論
triggers:
  - 反組譯一支 DOS 程式，符號尾端帶一個底線，想知道是哪一代 Watcom
  - 想快速判斷手上的程式是 16 位元還是跑在 DOS extender 上
  - 看到 `__PTS`、`__STK` 這類符號，想知道它們各自代表什麼年代
  - 想知道 Watcom 從 1988 到 1992 之間改了哪些會留下痕跡的東西
symbols: [__STK, __PTS, __PTC, __PIA, __PCA, __I4M, __I4D, __IsTable, __psp, __osmajor, __STACKTOP, __curbrk, __dynend, __StartTime, CLIBM_TEXT, CLIB3R, DOS4GW, int31h]
related: [watcom/watcom65-runtime, watcom/watcom386-extenders, watcom/dos4gw-startup, overview/era-and-libraries]
---

# 拿到一支 Watcom 編譯的程式，怎麼判斷是哪一代

## 結論

前面三篇分別拆了 6.5（1988，16 位元）、7.0（1989，第一代 32 位元）與 9.01（1992，DOS/4GW 年代）。
這一篇把它們收成一張判斷表。

判斷分兩段走，因為**前三題只分得出「16 位元」與「32 位元」，分不出 32 位元的兩代**：

**第一段：是不是 Watcom、是哪一邊**

1. **符號尾端有沒有一個底線？** 有，就是 Watcom 的暫存器呼叫慣例（`strlen_`、`main_`）。
   沒有的話還不能排除 Watcom：它的堆疊慣例版本不加尾端底線。
   **不過那一版的一般函式長什麼樣，我們沒讀到**——只在啟動碼的少數內部符號上看過規律
   （前綴的底線數不動、只改尾端），照那條規律推，一般函式在堆疊慣例下應該是完全沒有底線的裸名，
   但這是外推，沒有實物佐證（那份函式庫解不開）。
2. **有沒有那組 far 指標 helper（`__PTS`、`__PTC`、`__PIA`、`__PCA`）？**
   有就是 16 位元那一代（6.5）——**這組符號是分水嶺**，而且只要讀符號名就看得到。
   但它只能單向用：**沒看到不代表是 32 位元**，可能只是那支程式沒做過 far 指標運算。
3. **沒有那組符號時的退路：看位元寬度。** 反組譯畫面上是 16 位元還是 32 位元的暫存器與指令
   （`EAX` 這類 32 位元暫存器、`66h`／`67h` 前綴的有無），以及執行檔標頭。
   這一題比較費工，所以排在第二題之後當備援，不當主線。

**第二段：32 位元的兩代怎麼分**

4. **執行檔開頭有沒有一段 16 位元的 stub，而且字串裡出現 DOS/4GW？**
   有就是 9.01 那個年代（1992 以後）。7.0 沒有這種形態——它連連結器都不含，
   執行檔由 Phar Lap 或另一家的工具產生，格式跟著那家走。
   輔助線索：`int 31h`（DPMI）呼叫在 9.x 年代的程式裡很常見，7.0 那一代還是各家私有介面。

換句話說：**前三題把程式分進「6.5」或「32 位元的某一代」，第四題才分開 7.0 與 9.01。**

<p align="center"><img src="../../img/watcom-lineage.svg" width="880" alt="三代 Watcom 的判斷流程與各自的特徵符號、段名與執行環境"></p>

## 判斷表

| 特徵 | 6.5（1988） | 7.0（1989） | 9.01（1992） |
|---|---|---|---|
| 位元寬度 | 16 | 32 | 32 |
| 記憶體模型 | 六種（由四個位元旗標組合出來） | flat（平坦：沒有段，指標就是 32 位元整數） | flat |
| 執行環境（DOS extender：讓 DOS 程式切進 32 位元保護模式的執行期元件，編譯器本身不含） | 純 DOS，不需要 | Phar Lap 或 A.I. Architects，**自己買** | DOS/4GW **隨附**，另支援 Phar Lap 與 Ergo |
| 函式符號 | `strlen_` | `strlen_` | （讀不到，延續前代推論） |
| 指標 helper `__PTS`／`__PTC`／`__PIA`／`__PCA` | **有**（程式用到才連進來） | 沒有（符號字典核對過） | 沒有（架構推論） |
| 32 位元乘除 helper `__I4M`／`__I4D` | **有** | 沒有（符號字典核對過） | 沒有（架構推論） |
| 堆疊檢查 `__STK` | 有 | 有 | （讀不到，延續前代推論） |
| 浮點轉換 `__I4FS`／`__ModF` | 有 | 有 | （讀不到，延續前代推論） |
| `__StartTime`（給 `clock` 用） | 沒有 | **有** | （讀不到，延續前代推論） |
| 程式碼段名 | medium 是 `CLIBM_TEXT`；large／huge 是「模組名＋`_TEXT`」 | （庫解不開） | （庫解不開） |
| 啟動碼向 DOS 歸還記憶體（`AH=4Ah`） | **有** | 沒有 | （讀不到） |
| 空指標哨兵檢查 | **有**（除 tiny 外五個模型） | 沒有 | （讀不到） |
| `int 31h`（DPMI：保護模式程式向 extender 要服務的標準介面） | 不可能有 | 沒看到 | 同年代的程式（DMX）大量使用；**9.01 自己沒直接證實** |
| 執行檔結構 | 單純的 MZ（DOS 執行檔開頭那兩個字母） | 交給 extender 的連結器決定 | 16 位元 stub（前導小程式，負責找到 extender 再交出控制權）＋ 32 位元本體 |

表裡有兩種「不知道」，原因不一樣，別混在一起：

- **「（讀不到）」**：9.01 磁片上的檔案用 Watcom 自家的壓縮封裝，我們的解碼器還沒寫完。
- **「（庫解不開）」**：7.0 與 9.01 的出貨函式庫是 Phar Lap 的 Easy OMF-386 變體，
  我們自寫的 OMF 解析器還沒支援那個變體。這是**格式問題，不是壓縮問題**。

另外，**9.01 那一欄除了明標「讀不到」的格子，其餘多半是從安裝腳本或同年代的程式（DMX）推的**，
不是直接讀到 9.01 自己的內容。詳見下方的證據強度表。

## 三個判準背後的道理

上面那四題是操作順序；這一節講的是**每個判準為什麼成立**——知道道理才知道它什麼時候會失效。

### 一、指標 helper 的有無

16 位元的 Watcom 有一整組 helper（編譯器自動插入的輔助函式，原始程式碼裡看不到它）
處理 far 指標：兩個指標相減、比較、指標加減整數
（依指標放在哪組暫存器分成兩套）。理由是段與位移讓同一個位址有多種寫法，不先正規化就不能比大小。

32 位元 flat 模型沒有段，指標就是整數，這些 helper 整組消失。同樣消失的還有 32 位元乘除——
386 有原生指令。

**所以看到 `__PTS`／`__PIA` 就是 16 位元那一代**，而且這個判準比「指標大小」好用：
指標大小要先正確判斷段的定址寬度，符號名則是直接讀得到的。

### ⚠ 這個判準只能單向用

helper 是**按需連結**的：程式沒做 far 指標的減法或比較，連結器就不會把那個模組拉進來。
所以：

- **看到 `__PTS`／`__PIA` → 可以斷定是 16 位元那一代。**
- **沒看到 → 什麼都不能斷定。** 可能是 32 位元，也可能是一支根本沒碰 far 指標運算的 16 位元程式
  （small 模型的程式尤其容易整組都沒有）。

**能單向用的判準仍然有價值**，只是要記得它答的是「是」而不是「不是」。
真的沒看到任何 helper 時，退回去看啟動碼那兩件事與執行檔結構。

（`__STK` 是編譯器在函式開場插入的，照理也會受編譯選項影響，
但手上這兩版的原廠文件裡**沒有找到**停用堆疊檢查的開關，所以這裡不下斷言。）

### 二、啟動碼做不做那兩件事

16 位元版的啟動碼會把多出來的記憶體還給 DOS（`AH=4Ah`），並在程式結束時掃一段哨兵看有沒有
被空指標寫壞。32 位元版兩件都不做。記憶體歸 extender 管，所以沒有還給誰的問題；
至於為什麼不再檢查空指標，合理的解釋是保護模式本身會攔截對位址 0 的寫入——
**這是推論，原廠沒說**。

反組譯時的具體形態：進入點附近如果出現「`AH` 設成 `4Ah` 再 `int 21h`」，接著不遠處又有
「`AH` 設成 `30h` 再 `int 21h`」（取 DOS 版本），那是 16 位元那一代。
（`int 21h` 是 DOS 的系統呼叫入口，功能編號放在 `AH`。）

### 三、執行檔的形狀

- **6.5**：普通的 MZ 執行檔。
- **7.0**：Watcom 自己不含連結器，執行檔長什麼樣由 extender 的連結器決定（我們沒有那些工具，
  所以這格是空的）。
- **9.01 以後**：典型的形態是「16 位元的 stub ＋ 32 位元本體」黏在一起，stub 負責找到 extender
  再把自己交出去。**stub 的內容我們沒讀到**（原始碼在壓縮封包裡，只知道它是 984 bytes），
  這個描述是從大小與檔名推的。那個年代的遊戲啟動時常見 DOS/4GW 字樣，是一般印象，
  本系列沒有做過遊戲清單普查。

## 年代之外還能看出什麼

**呼叫慣例**：從 7.0 起，同一套函式庫出暫存器版與堆疊版兩份，檔名帶 `r` 或 `s`。
所以看到 `_r`／`_s` 結尾的 DOS 函式庫檔名（例如 1993 年的音效函式庫 DMX 的兩個庫），
多半是這條線上的東西。**但「`s` 走堆疊」這件事原廠文件沒有明說**，
是從旗標與命名推的（見 [6.5 那篇](watcom65-runtime.md) 的證據段）。

**編譯器本身的位元寬度**：7.0 的編譯器與反組譯器都是 16 位元的 MZ 執行檔，
只是產生 32 位元的碼。「32 位元編譯器」講的是輸出，不是工具自己。

## 這一系列的證據強度，逐版不同

這點對讀者怎麼採信結論很重要：

下表的「最高等級」指**各版目前能達到的天花板**，不是整篇文章的評級——
依本庫的規範，文章前置欄位的 `evidence` 取全篇**最弱**的一級，所以那幾篇標的都是「強推論」。

| 版本 | 有什麼 | 最高等級 |
|---|---|---|
| 6.5 | 原廠文件、**啟動碼原始碼**、函式庫可解開並逐模組反組譯 | 已證實（實測） |
| 7.0 | 原廠文件、**啟動碼原始碼**、編譯器可在無頭 DOS 執行器裡實跑 | 已證實（實測） |
| 9.01 | 原廠安裝腳本、封包成員清單 | 已證實（原文），內容層面**只到檔名與大小** |

9.01 那一欄的限制來自 WPK 封裝還沒解開。本篇表格裡 9.01 的每一格，來源只有三種：

1. **原廠安裝腳本**（extender、目標平台、封包與目錄的對應）——這類最可靠。
2. **同年代的程式**（DMX 用 DPMI 那幾條）——講的是那個年代的程式，不是 9.01 編譯器本身。
3. **延續 6.5／7.0 的推論**（符號形態、helper 的有無那幾格）——**完全沒有直接證據**，
   只是「架構沒變、前一版這樣，這一版應該也是」。表裡標「延續前代推論」的就是這一類。

## 證據與未知

**已證實（實測）**：6.5 與 7.0 的符號統計、helper 的有無與語意、段名規律、
7.0 自己編出來的程式長什麼樣。方法是自寫的 OMF 解析器、原廠反組譯器，
以及在無頭 DOS 執行器裡實際編譯。

**已證實（原文）**：三版的記憶體模型與函式庫分份、7.0 的 extender 依賴與兩種呼叫慣例、
9.01 支援的三家 extender 與 DOS/4GW 隨附、啟動碼做與不做哪些事（6.5 與 7.0 有原始碼）。

**強推論**（全篇最弱的一級）：

- 9.01 的啟動碼行為與它是否走 DPMI——推自同年代的 DMX 與安裝腳本。
- **9.01 欄裡標「延續前代推論」的每一格**（符號形態、`__STK`、浮點 helper、`__StartTime`、
  兩組消失的 helper）——那一版的函式庫與啟動碼都還沒解開，這些是從 7.0 外推的。
- 「`s` 版走堆疊」——原廠文件沒明說。
- 「7.0 的執行檔形狀由 extender 的連結器決定」——我們沒有那些連結器，沒有實物可看。
- 「9.01 的執行檔是 stub ＋ 本體」——stub 的內容沒讀到，從檔名與大小推的。
- 「堆疊慣例的一般函式是裸名」——只在啟動碼的內部符號上驗過規律，一般函式庫解不開。
- 「保護模式會攔截空指標寫入，所以 32 位元版不再檢查」——原廠沒說。

**未知**：

- **9.01 的封包內容解不開**，所以表裡三格是空的。卡點：照原始碼移植的解碼器輸出長度正確
  但內容是亂碼；已排除排序順序這個假設（照原廠的 `qsort` 完整移植後結果沒變），
  剩下三個可疑處寫在私有筆記裡。
- **8.0 沒有取得**，7.0 到 9.01 之間的變化是跳著看的。
- 9.5 與更後面的版本沒碰。
- 沒有任何一版做過位元組簽章（Watcom 這一階段不做），所以這篇的判準全部是符號與結構層級，
  不是位元組層級。
